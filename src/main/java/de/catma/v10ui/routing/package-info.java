/**
 * Routable classes which just hold @Route annotations and delegate to the proper view via DI (guice).
 * This is required to implement a stateful routing.
 *
 * @author db
 */
package de.catma.v10ui.routing;